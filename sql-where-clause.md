hidden data retrieval in WHERE clause

i applied payload +OR+1=1-- right after to the product category directly from the URL  by first closing it with a single string '

OR 1=1 forces the condition to be true which makes the WHERE filter to stop filtering and show whats hidden

i used -- to remove the initial query to keep things valid to avoid errors
